# express 断点续传

问题：大文件上传，暂停上传，上传失败重新上传

```shell
nvm use $(Get-Content .nvmrc)
```

```shell
npm install
```

```shell
npm run start
```

直接打开 http://localhost:8000/ 上传即可

如果没有上传的资源，可以使用 /example/1.jpg

